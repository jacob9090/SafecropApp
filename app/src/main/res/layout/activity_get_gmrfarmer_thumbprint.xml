<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    tools:context=".sustain.profiling.GetGMRFarmerThumbprintActivity">

    <ImageView
        android:id="@+id/gmrTpImageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:padding="20dp"
        android:src="@drawable/fingerprint_brown"
        android:background="@android:color/darker_gray"
        android:contentDescription="@string/selected_or_captured_image" />

    <Button
        android:id="@+id/gmrTpCaptureOrSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/selected_or_captured_image"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/gmrTpbtnOk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/ok"
        android:layout_marginTop="16dp" />

</LinearLayout>